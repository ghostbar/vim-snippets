snippet     Readable
abbr        ReadableStream
options     head
  var ${1:rs} = new (require('stream').Readable)();${0}

snippet     _read
abbr        _read = fn(size){
options     word
  _read = function (${1:#:size}) {
    ${0}
  };
